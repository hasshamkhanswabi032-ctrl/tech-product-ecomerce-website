{
  "name": "ecommerce-monorepo",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm --prefix frontend install",
    "build": "npm --prefix frontend run build"
  }
}
